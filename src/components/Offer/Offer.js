import React from "react";
import "./Offer.scss";

const Offer = (props) => {
  return (
    <React.Fragment>
      <section className="offer"></section>
    </React.Fragment>
  );
};
export default React.memo(Offer);
